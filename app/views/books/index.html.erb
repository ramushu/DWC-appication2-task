<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
        <h2>User info</h2>
        <%= image_tag @user.get_image %>
        <table>
          <tbody>
            <tr>
              <th>name</th>
              <th><%= @user.name %></th>
            </tr>
            <tr>
              <th>introduction</t>
              <th><%= @user.introduction %></th>
            </tr>
          </tbody>
        </table>
        <div class="user-edit">
          <%= link_to edit_user_path(@user), class: "btn btn-outline-secondary btn-block" do %>
            <i class="fas fa-user-cog"></i>
          <% end %>
        </div>

        <h2>New book</h2>
        <%= form_with model: @book, url: "/books", method: :post do |f| %>
          <div class="new-book-field">
            <%= f.label :title, "Title" %>
            <%= f.text_field :title %>
          </div>

          <div class="new-book-field">
            <%= f.label :body, "Opinion" %>
            <%= f.text_area :body %>
          </div>

          <div class="new-book-action">
            <%= f.submit "Create Book" %>
          </div>
        <% end %>
    </div>

    <div class="col-md-9">
      <div class="book-contents-right">
        <h2>Books</h2>
      </div>
    </div>

  </div>
</div>